
<%= csrf_meta_tags %>

<div class="container-fluid">
    <div class="row content"> 
        <div class="col-sm-4 sidenav">
        </div>
        <div class="col-sm-4 "> 
        
         <%= form_for [@user] do |f| %>
        <div class="form-group">
          <%= f.label :patientlist, "Add patient ID" %>
          <%= f.text_field :patientlist, class:"form-control" %>
        </div>  
        
        <div class="form-group">
          <%= f.submit "Submit", class: "btn btn-default btn-warning btn-block" %>
        </div>
        
        
    <%end%>

        <%= link_to 'Back to Profile', user_path(current_user.id), class:"btn btn-warning btn-block" %> 

    </div>
</div>
